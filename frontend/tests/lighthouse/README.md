# Lighthouse Tests

This folder contains instructions and output for running Lighthouse audits locally.

Run locally:

```powershell
# start the app in another terminal
cd frontend; npm start

# in this folder or project root
cd frontend
npm run lighthouse
```

Reports will be written to `frontend/tests/lighthouse/report.html` and JSON outputs will be placed in `frontend/tests/lighthouse/`.
